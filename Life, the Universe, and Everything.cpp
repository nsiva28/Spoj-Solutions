#include <bits/stdc++.h>

using namespace std;

void test_case() {
    int n;
    while (cin >> n) {
        if (n == 42)break;
        cout << n << '\n';
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t = 1;
//    cin >> t;
    while (t--) {
        test_case();
    }
    return 0;
}
